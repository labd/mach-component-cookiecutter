import { CartUpdateAction } from '@commercetools/platform-sdk'

// TODO: This is an example action that is invalid (on purpose), please change me :)
export const exampleCartAction = (): CartUpdateAction =>
  ({
    action: 'exampleAction',
    examplePayload: 0,
  } as any)
